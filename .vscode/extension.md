```bash
    # remove line [this is must be need otherwise get extra blank line every end of the .html file]
     DoCode.vscode-remove-final-newlines
    # formater [must be need for format the gulp generated code.]
     esbenp.prettier-vscode
```
